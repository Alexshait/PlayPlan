<Window x:Class="PlayPlan.Views.LogonView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:viewmodels="clr-namespace:PlayPlan.ViewModels" 
        xmlns:local="clr-namespace:PlayPlan" 
        d:DataContext="{d:DesignInstance Type=viewmodels:LogonViewModel}"
        mc:Ignorable="d"
        Title="PlayPlan Logon" Height="450" Width="800" WindowStyle="SingleBorderWindow">
    <Grid Margin="30">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <WebBrowser Grid.Row="0" Grid.Column="0" viewmodels:LogonWebBrowserBinding.BindableSource="{Binding WebAddress}"
            ScrollViewer.HorizontalScrollBarVisibility="Disabled" 
            ScrollViewer.VerticalScrollBarVisibility="Disabled"
            x:Name="LogonWebBrowser" Margin="0,0,-663,4" />
        <Button Content="test" />
    </Grid>
</Window>
